# nodets-canil

### Pré-requisitos globais:

`npm i -g nodemon typescript ts-node`

### Instalação:

`npm install`

### Variaveis de ambiente:

`PORT=numerodaporta`

### Para rodar o projeto:

`npm run dev`
